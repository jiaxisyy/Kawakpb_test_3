<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="login_checkbox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/login_checkbox_selector</item>
    </style>

    <style name="RadioButton_main">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textSize">18px</item>
        <item name="android:textColor">#A3D0D9</item>
        <item name="android:button">@null</item>
    </style>

    <style name="left_menu_selector">
       
        <item name=" android:layout_marginLeft">39px</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#03A3EB</item>
        <item name="android:textSize">18px</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>


    </style>
</resources>
